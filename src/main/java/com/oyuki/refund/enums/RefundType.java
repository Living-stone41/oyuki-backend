package com.oyuki.refund.enums;

public enum RefundType {

    /*
     * The complete paid amount is returned.
     */
    FULL,

    /*
     * Only part of the paid amount is returned.
     */
    PARTIAL
}