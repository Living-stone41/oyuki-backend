(function(){'use strict';
const O=window.Oyuki,form=document.getElementById('marketAgentActivationForm'),error=document.getElementById('activationError'),button=document.getElementById('activateButton');
const params=new URLSearchParams(location.search);
if(params.get('email'))document.getElementById('email').value=params.get('email');
if(params.get('phone'))document.getElementById('phoneNumber').value=params.get('phone');
form?.addEventListener('submit',async e=>{e.preventDefault();error.classList.remove('show');const payload=Object.fromEntries(new FormData(form).entries());if(!payload.email&&!payload.phoneNumber){error.textContent='Enter the email or phone number used for this account.';error.classList.add('show');return;}if(!payload.emailOtp&&!payload.phoneOtp){error.textContent='Enter at least one OTP.';error.classList.add('show');return;}if(payload.password!==payload.confirmPassword){error.textContent='Passwords do not match.';error.classList.add('show');return;}try{button.disabled=true;button.textContent='Activating…';await O.Api.post('/auth/activate-market-agent',payload,false);O.Toast.show('Market Agent account activated. You can now log in.','success');setTimeout(()=>location.href='login.html',900);}catch(err){error.textContent=err.message||'Unable to activate Market Agent account.';error.classList.add('show');}finally{button.disabled=false;button.textContent='Verify and activate';}});})();
